# 火山引擎视频生成中转服务 - 最终依赖
# 简化版本，避免依赖问题

# Web框架
Flask==2.3.3

# 网络请求
requests==2.31.0
volcengine>=1.0.98
python-dotenv==1.0.0
# Flask依赖
Werkzeug==2.3.7
Jinja2==3.1.3
itsdangerous==2.1.2
click==8.1.7

# 注意：移除了gunicorn依赖
# 使用Flask内置服务器，确保部署成功
